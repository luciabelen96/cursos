cadena_1 = "versátil"
cadena_2 = "Python"
cadena_3 = "es un lenguaje"
cadena_4 = "de programación"
resultado = print(cadena_2 + " " + cadena_3 + " " + cadena_4 + " " + cadena_1)

# Ingreso de datos
número_primero = int(input("Ingresa el primer número: ")) # boton a completar
número_segundo = int(input("Ingresa el segundo número: ")) # 2do boton a completar

# Operaciones con los datos
resultado = número_primero - número_segundo

# Salida de datos
print("Mi cálculo es", resultado )

######################## Segundo micro desafío

# Declaración de datos conocidos
PARTIDOS_JUGADOS = 20
PUNTOS_PARTIDO_GANADO = 3
PUNTOS_PARTIDO_EMPATADO = 1
PUNTOS_PARTIDO_PERDIDO = 0

# Entrada de datos
partidos_ganados = int(input("¿Cuántos partidos ganó el equipo? "))
partidos_empatados = int(input("¿Cuántos partidos empató el equipo? "))
partidos_perdidos = int(input("¿Cuántos partidos perdió el equipo? "))

# Operaciones
puntos_totales = (
    partidos_ganados * PUNTOS_PARTIDO_GANADO
    + partidos_empatados * PUNTOS_PARTIDO_EMPATADO
    + partidos_perdidos * PUNTOS_PARTIDO_PERDIDO
)
promedio = puntos_totales / PARTIDOS_JUGADOS

# Salida de datos
print(f"El promedio final de puntos es {promedio}")

############################################3

CADENA = "TEXTO"

print(CADENA[0])
print(CADENA[1])
print(CADENA[2])
print(CADENA[3])
print(CADENA[4])

###########################################
# cadena de texto
CADENA = "TEXTO esto es una prueba"
# [ inicio : paro : paso]
print(CADENA[::-1])

# texto entre comillas 
print('esto es un "texto"')
##############################################

lista = [2, 4, 5, 6]

print(lista[0])
print(lista[1])
print(lista[2])
print(lista[3])

#############################################

lista = [20, 41, 5, 6, "cadenas", [2, 3]]
lista.append(10)
print(len(lista))
print(lista)
lista.pop()
print(lista)
lista.remove(20)
print(lista)
print(lista[4])
mi_tres = lista[4][1]
print(mi_tres)

###########################################################################
# Impresion de forma cruda o raw
# quiero que python me ignore los caracteres especiales
# pe establecer el directorio de trabajo
print(r"C/nombre/directorio")
#como escribir una cadena de texto de varias lineas: utilizando """ (3 comillas, donde cada comilla representa un num de renglon)
print("""esto
es
un texto""")

# Listas sirve para agrupar distintos tipos de datos
mi_lista = [1,2,3,4] # homgenea numerica
mi_lista = ["hola","que tal"] #homogenea string
mi_lista = [1,2,-5,"hola"] # heterogenea
# substring un elemento de la lista
mi_lista[0] # cero es el primer elemento de la lista
mi_lista[-1] # ultimo elemento de la lista
# slicing (quedarme con una parte de los datos)
mi_lista[0:2]
mi_lista[-2:]
#Agregar datos a una lista
mi_lista = mi_lista + [3,6,9,0]
# borrar elementos de la lista
mi_lista[:3] = [] # tomame los primeros tres elementos y reemplazalos por una lista vacia, de esta manera entiende que vamos a borrar esos elementos.
mi_lista.clear() # sirve para borrar los elementos de una lista

## Funciones más importantes para las listas i
append = mi_lista.append(5)
mi_lista.append(2*3) #permite agregar elemntos al final de la lista 
#len longitud del vector
len(mi_lista)    
# pop extrae el último elemento de la lista
nuevo = mi_lista.pop() # tambien le podemos especificar el valor que queremos extraer
nuevo = mi_lista.pop(2) 
#count permite saber cuantas veces se repite determinado item en la lista
mi.lista.count(2)
# index me marca la primer ocurrencia de un valor en la lista (el lugar que ocupa el elemento la primera vez que aparece)
mi_lista = [1,2,3,2,2]
mi_lista.index(2) # me va a marcar el 1 pq es la pimera posición que ocupa. 
mi_lista.index(5) # me va a tirar error pq el elemento no se encuentra en la lista
